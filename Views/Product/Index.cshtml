@model Assignment_12_1.Models.IndexViewModel

@**@

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Products.First().Id);
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Products.First().PName);
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Products) {
        <tr>
            <td>@Html.DisplayFor(productlitem => item.Id)</td>
            <td>@Html.DisplayFor(profuctitem => item.PName)</td>
           <td>
                <a asp-action="Details" asp-controller="Product" asp-route-id ="@item.Id">Details</a>
            </td>
                <td>
                    <a asp-action="Edit" asp-controller="Product" asp-route-id="@item.Id" asp-route-name="@item.PName">Edit</a>
                </td>
                <td>
                    <a asp-action="Delete" asp-controller="Product" asp-route-id="@item.Id">Delete</a>
                </td>
        </tr>
}
    </tbody>
</table>
